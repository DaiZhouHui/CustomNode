name: Update Node Index

on:
  push:
    # 只有当非生成的代码文件发生变化时才触发
    paths:
      - '**'
      - '!index.html'
      - '!update-index.html'
      - '!files_info.json'
    branches: [ main ]
  schedule:
    # 每天凌晨2点自动更新 (UTC)
    - cron: '0 2 * * *'
  workflow_dispatch: # 允许手动按钮触发

permissions:
  contents: write # 必须赋予写权限，否则无法提交回 main

jobs:
  update-index:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 获取完整历史，而不仅仅是最近一次提交
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.14'  # 指定所需的 Python 版本
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 假设 requirements.txt 在根目录
        pip install -r requirements.txt
        
    - name: Generate index files
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        REPO_OWNER: ${{ github.repository_owner }}
        REPO_NAME: ${{ github.event.repository.name }}
      run: |
        # ✅ 修复点：指定正确的脚本路径
        # 注意：我们在根目录运行这个命令，生成的文件也会出现在根目录
        # 这样正好符合 GitHub Pages 的要求
        python scripts/generate-index.py
        
    - name: Commit and Push to Main
      # ✅ 修复点：既然你只有 main 分支，我们直接把生成的文件提交回 main
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "🤖 Auto-update index html"
        file_pattern: 'index.html update-index.html files_info.json'
        branch: main
        skip_dirty_check: false